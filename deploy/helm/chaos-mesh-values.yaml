# Optional values for installing the official Chaos Mesh chart on nodes that
# use containerd. Adapt these settings for your Kubernetes environment.

chaosDaemon:
  runtime: containerd
  socketPath: /run/containerd/containerd.sock

dashboard:
  create: true
  securityMode: true
  service:
    type: ClusterIP
